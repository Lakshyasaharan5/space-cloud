# SpaceCloud
Google Drive like distributed cloud storage platform

### Start datanode instance
```
./mvnw spring-boot:run -Dspring-boot.run.arguments="--server.port=8081 --storage.path=data-node1"
```
